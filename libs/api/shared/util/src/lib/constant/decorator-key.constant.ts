/**
 * The string to use to mark a controller endpoint as public.
 */
export const IS_PUBLIC_KEY = 'isPublic';

/**
 * The string to use to access role metadata.
 */
export const ROLE_KEY = 'role';

/**
 * The string to use to mark a controller endpoint as OK to access past the email verification time limit.
 */
export const UNVERIFIED_OK_KEY = 'unverifiedOk';
