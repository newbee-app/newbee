/**
 * Shortened URL endpoints for use in the frontend, to save users from very long URLs.
 * Only for use in the frontend.
 */
export enum ShortUrl {
  Organization = 'o',
  Team = 't',
  Doc = 'd',
  Qna = 'q',
  Member = 'm',
}
