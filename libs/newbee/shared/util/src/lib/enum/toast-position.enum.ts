/**
 * The supported toast positions along the x-axis.
 */
export enum ToastXPosition {
  Start = 'start',
  Center = 'center',
  End = 'end',
}

/**
 * The supported toast positions along the y-axis.
 */
export enum ToastYPosition {
  Top = 'top',
  Middle = 'middle',
  Bottom = 'bottom',
}
