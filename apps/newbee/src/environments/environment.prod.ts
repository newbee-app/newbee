export const environment = {
  production: true,
};

/**
 * Modules that should only be included in prod builds.
 */
export const extModules = [];

/**
 * The base API URL.
 */
export const baseApiUrl = 'http://localhost:3333';
